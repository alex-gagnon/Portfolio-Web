version: "3"
services:
    nginx:
        image: agagnon227/nginx
        container_name: nginx
        restart: always
        build:
            context: ./nginx
            dockerfile: Dockerfile
        ports:
            - "8080:80"
    api:
        image: agagnon227/portfolio-api
        container_name: api
        build:
            context: ./server
            dockerfile: Dockerfile
        volumes:
            - /app/node_modules
            - ./server:/app
    client:
        image: agagnon227/portfolio
        container_name: client
        build:
            context: ./client
            dockerfile: Dockerfile
        volumes:
            - /app/node_modules
            - ./client:/app
        
